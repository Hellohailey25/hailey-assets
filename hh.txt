<!-- Сквозная шапка HAILEY — 1:1 с карточкой PDP (стиль/анимация/липкий blur).
     Тема: light→white→dark, запоминается (localStorage 'hailey-theme') — на весь сайт.
     Корзина: открывает корзину текущей страницы (#openCart карточки / #cartBtn витрины),
     счётчик читает общую корзину (localStorage 'hailey_cart_v1'). Прячет родную сумку Tilda Store. -->
<style>
  .hh *{margin:0;padding:0;box-sizing:border-box}
  .hh{--paper:#F2F0EB;--paper2:#EAE7E0;--card:#FBFAF7;
    --ink:#23262C;--ink-2:#5C626B;--ink-3:#8A9099;
    --line:rgba(35,38,44,.14);--line-soft:rgba(35,38,44,.08);--accent:#2B3648;
    --disp:"Helvetica Neue",Helvetica,Arial,sans-serif;
    color:var(--ink);font-family:var(--disp);-webkit-font-smoothing:antialiased;line-height:1.55;
    position:relative;z-index:1000}
  /* Верхняя тонкая плашка (.top) в потоке — при скролле уходит вверх; липнет только .bar. */
  .hh-sp{height:0}
  @media(prefers-color-scheme:dark){.hh{--paper:#16181d;--paper2:#1d2027;--card:#1c1f26;--ink:#ECE9E1;--ink-2:#a2a8b3;--ink-3:#727a86;--line:rgba(236,233,225,.14);--line-soft:rgba(236,233,225,.07);--accent:#9CB0D0}}
  :root[data-theme="dark"] .hh{--paper:#16181d;--paper2:#1d2027;--card:#1c1f26;--ink:#ECE9E1;--ink-2:#a2a8b3;--ink-3:#727a86;--line:rgba(236,233,225,.14);--line-soft:rgba(236,233,225,.07);--accent:#9CB0D0}
  :root[data-theme="light"] .hh{--paper:#F2F0EB;--paper2:#EAE7E0;--card:#FBFAF7;--ink:#23262C;--ink-2:#5C626B;--ink-3:#8A9099;--line:rgba(35,38,44,.14);--accent:#2B3648}
  :root[data-theme="white"] .hh{--paper:#FFFFFF;--paper2:#F4F4F5;--card:#FFFFFF;--ink:#1A1C22;--ink-2:#565B63;--ink-3:#8A9099;--line:rgba(20,22,28,.12);--accent:#2B3648}
  .hh a{color:inherit;text-decoration:none}
  .hh button{font-family:inherit;cursor:pointer;border:none;background:none;color:inherit}
  .hh svg{display:block}
  .hh .wrap{width:min(1440px,92vw);margin:0 auto}
  .hh .top{border-bottom:1px solid var(--line-soft);background:var(--paper)}
  .hh .top .wrap{display:flex;justify-content:center;gap:10px;padding:8px 0;font-size:10.5px;letter-spacing:.16em;text-transform:uppercase;color:var(--ink-3);flex-wrap:wrap}
  .hh .top .sep{width:3px;height:3px;border-radius:50%;background:var(--line);align-self:center}
  .hh .bar{background:color-mix(in srgb,var(--paper) 88%,transparent);backdrop-filter:blur(12px);transition:box-shadow .2s}
  .hh .bar.stuck{position:fixed;top:0;left:0;right:0;z-index:1000;box-shadow:0 1px 0 var(--line-soft),0 8px 24px rgba(0,0,0,.06)}
  .hh .hd{display:flex;align-items:center;justify-content:space-between;height:66px;gap:20px}
  .hh .logo{font-size:22px;letter-spacing:.34em;font-weight:700;padding-left:.5em;display:flex;align-items:baseline;cursor:pointer}
  /* «Hello» — подпись при вордмарке: лёгкий вес и приглушённый цвет, размер в em (тянется за логотипом) */
  .hh .logo i{font-family:inherit;font-style:normal;font-weight:400;font-size:.78em;letter-spacing:.04em;margin-right:.42em;color:var(--ink-2)}
  .hh nav{display:flex;gap:18px;font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-2);white-space:nowrap}
  .hh nav a{cursor:pointer}
  .hh nav a:hover{color:var(--ink)}
  .hh .htools{display:flex;gap:18px;align-items:center;font-size:12px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-2)}
  .hh .htools a.s:hover{color:var(--ink)}
  /* --- живые подсказки поиска (мини-фото) --- */
  .hh .s{position:relative}
  .hh-sug{position:absolute;top:calc(100% + 9px);right:0;width:min(340px,86vw);background:var(--card);
    border:1px solid var(--line);border-radius:12px;box-shadow:0 20px 50px rgba(0,0,0,.18);padding:6px;
    display:none;z-index:1001;max-height:min(72vh,440px);overflow-y:auto;text-transform:none;letter-spacing:0}
  .hh-sug.on{display:block}
  .hh-sug .si{display:flex;gap:11px;align-items:center;padding:8px 9px;border-radius:9px;cursor:pointer}
  .hh-sug .si:hover,.hh-sug .si.act{background:var(--paper2)}
  .hh-sug .si img{width:40px;height:50px;object-fit:cover;border-radius:5px;flex:0 0 auto;background:var(--paper2)}
  .hh-sug .si .sn{font-size:13px;line-height:1.32;color:var(--ink);overflow:hidden;
    display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical}
  .hh-sug .si .sp{font-size:13px;font-weight:700;color:var(--ink);margin-top:3px}
  .hh-sug .sall{display:block;text-align:center;padding:11px;font-size:10.5px;letter-spacing:.14em;
    text-transform:uppercase;color:var(--ink-2);border-top:1px solid var(--line-soft);margin-top:4px;cursor:pointer}
  .hh-sug .sall:hover{color:var(--ink)}
  .hh-sug .snone{padding:16px 12px;text-align:center;color:var(--ink-3);font-size:13px}
  .hh .theme{width:34px;height:34px;border:1px solid var(--line);border-radius:5px;display:flex;align-items:center;justify-content:center;font-size:14px;color:var(--ink-2);flex:0 0 auto;transition:border-color .2s,color .2s}
  .hh .theme:hover{border-color:var(--accent);color:var(--ink)}
  .hh .cartlink{display:flex;align-items:center;gap:7px;font-size:12px;letter-spacing:.1em;text-transform:uppercase;font-weight:600;color:var(--ink)}
  .hh .cartlink:hover{color:var(--accent)}
  .hh .cartlink .cart-ic{width:16px;height:16px;flex:none;margin-top:-1px}
  .hh .cartlink .cc{min-width:20px;height:20px;padding:0 6px;border-radius:11px;background:var(--accent);color:#fff;font-size:11px;font-weight:700;display:flex;align-items:center;justify-content:center;transform:scale(0);transition:.2s}
  .hh .cartlink .cc.on{transform:scale(1)}
  /* Избранное в шапке (десктоп) — тот же контурный значок, что на карточках каталога (.hc .fav) */
  .hh .favlink{display:flex;align-items:center;gap:6px;color:var(--ink);flex:0 0 auto}
  .hh .favlink svg{width:17px;height:17px;fill:none;stroke:currentColor;stroke-width:1.7;stroke-linecap:round;stroke-linejoin:round}
  .hh .favlink:hover{color:var(--accent)}
  .hh .favlink.has svg{fill:#e2574c;stroke:#e2574c}
  .hh .favlink .fc{min-width:19px;height:19px;padding:0 6px;border-radius:10px;background:var(--accent);color:#fff;
    font-size:10.5px;font-weight:700;display:flex;align-items:center;justify-content:center;transform:scale(0);transition:.2s}
  .hh .favlink .fc.on{transform:scale(1)}
  /* строка «Избранное» в бургер-меню (на телефоне значка в шапке нет — место занимают поиск и корзина) */
  .hh .mnu a.mfav{display:flex;align-items:center;gap:9px}
  .hh .mnu a.mfav svg{width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:1.7;stroke-linecap:round;stroke-linejoin:round}
  .hh .mnu a.mfav .fc{min-width:19px;height:19px;padding:0 6px;border-radius:10px;background:var(--accent);color:#fff;
    font-size:10.5px;font-weight:700;display:none;align-items:center;justify-content:center;letter-spacing:0;margin-left:auto}
  .hh .mnu a.mfav .fc.on{display:flex}
  /* Шапка тянула страницу вбок на средних экранах: 8 пунктов меню (694px) + логотип + поиск + корзина
     не помещались в .wrap. Лечим в два шага, чтобы не терять горизонтальное меню на ноутбуках:
     до 1300px убираем «Контакты» и «О бренде» (они есть в бургере и в подвале) и ужимаем шаг,
     а до 1100px навигацию целиком отдаём бургеру. */
  @media(max-width:1300px){.hh nav .nx{display:none}.hh nav{gap:14px;font-size:11.5px;letter-spacing:.05em}}
  @media(max-width:1100px){.hh nav{display:none}}
  /* Телефон: логотип + поиск + корзина не влезают в 375px и выдавливают корзину за экран.
     Ужимаем логотип, поиск делаем резиновым, у корзины оставляем иконку со счётчиком. */
  @media(max-width:560px){
    .hh .hd{height:60px;gap:10px}
    .hh .logo{font-size:16px;letter-spacing:.2em;padding-left:.16em}
    .hh .htools{gap:10px;flex:1;min-width:0;justify-content:flex-end}
    .hh .s{flex:1;min-width:0}
    /* у поля поиска ширина в inline-стиле и её же меняют onfocus/onblur — бьём по id с !important */
    .hh form.s #hhQ{width:100%!important;max-width:100%!important;min-width:0!important}
    .hh .theme{width:30px;height:30px;font-size:13px}
    .hh .cartlink .ct{display:none}
    .hh .cartlink{gap:5px}
    .hh .favlink{display:none}   /* на телефоне избранное живёт в бургер-меню */
    .hh .top{font-size:9.5px}
  }
  /* --- бургер-меню (только телефон): на ≤820px обычная навигация скрыта --- */
  .hh .burg{display:none;width:32px;height:32px;flex:0 0 auto;align-items:center;justify-content:center;
    border:1px solid var(--line);border-radius:5px;background:transparent;cursor:pointer;color:var(--ink)}
  .hh .burg span{display:block;width:15px;height:1.5px;background:currentColor;position:relative}
  .hh .burg span:before,.hh .burg span:after{content:"";position:absolute;left:0;width:15px;height:1.5px;background:currentColor}
  .hh .burg span:before{top:-5px}.hh .burg span:after{top:5px}
  .hh .mnu{position:fixed;top:0;bottom:0;left:0;width:min(84vw,320px);background:var(--paper);z-index:1002;
    transform:translateX(-101%);transition:transform .26s ease;overflow-y:auto;padding:18px 20px 30px;
    box-shadow:0 0 40px rgba(0,0,0,.18);display:flex;flex-direction:column;
    /* панель — тоже <nav>, поэтому на неё падают правила .hh nav (gap/nowrap): гасим явно */
    gap:0;white-space:normal}
  .hh .mnu.on{transform:none}
  .hh .mnu .mh{display:flex;align-items:center;justify-content:space-between;margin-bottom:22px}
  /* логотип в меню = логотип шапки; специфичность выше .mnu a, чтобы не поймать капс/подчёркивание пунктов */
  .hh .mnu .mh .mlogo{display:flex;align-items:baseline;padding:0;border-bottom:none;text-transform:none;
    font-size:16px;letter-spacing:.2em;font-weight:700;color:var(--ink)}
  .hh .mnu .mh .mlogo i{font-style:normal;font-weight:400;font-size:.78em;letter-spacing:.04em;margin-right:.42em;color:var(--ink-2)}
  .hh .mnu .cl{width:32px;height:32px;border:none;background:none;color:var(--ink-2);font-size:20px;cursor:pointer;line-height:1}
  .hh .mnu a{display:block;padding:14px 0;font-size:14px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink);border-bottom:1px solid var(--line-soft)}
  .hh .mnu a:active{color:var(--accent)}
  .hh .mnu .grp{font-size:10.5px;letter-spacing:.16em;text-transform:uppercase;color:var(--ink-3);margin:22px 0 4px}
  .hh .mnu .cts a{font-size:14px;color:var(--ink-2);border-bottom:none;padding:9px 0}
  .hh .ovl{position:fixed;inset:0;background:rgba(0,0,0,.42);z-index:1001;opacity:0;pointer-events:none;transition:opacity .26s}
  .hh .ovl.on{opacity:1;pointer-events:auto}
  /* выбор темы внутри меню: видно, какая включена (иконка-цикл в шапке этого не показывала) */
  .hh .mnu .thm{display:flex;gap:6px;margin-top:8px}
  .hh .mnu .thm button{flex:1;padding:10px 4px;font-size:10.5px;letter-spacing:.06em;text-transform:uppercase;
    border:1px solid var(--line);border-radius:6px;color:var(--ink-2);background:transparent;transition:border-color .18s,color .18s}
  .hh .mnu .thm button.on{border-color:var(--accent);color:var(--ink);background:var(--paper2);font-weight:700}
  /* включаем бургер там же, где прячется nav (≤820px) — правило идёт ПОСЛЕ .burg{display:none}, иначе не перебьёт.
     Иконку темы на телефоне убираем: её роль берёт блок «Тема» в меню, а в шапке освобождается место. */
  @media(max-width:1100px){.hh .burg{display:flex}}
  @media(max-width:820px){.hh .theme{display:none}}
  .t706__carticon{display:none!important}
  /* прячем вшитые шапки страниц — их заменяет эта сквозная (корзина/тема остаются для связки) */
  .hp>.top,.hp>header{display:none!important}
  /* прячем плавающую корзину витрины/каталога — корзину даёт хедер (клик по ней открывает тот же drawer) */
  .hc .cartbtn{display:none!important}
  /* === СКВОЗНАЯ ТЕМА: переключатель красит ВСЕ блоки (не только шапку) === */
  :root[data-theme="dark"] .hlxh{--paper:#16181d;--ink:#ECE9E1;--ink-2:#a2a8b3;--ink-3:#727a86;--accent:#9CB0D0}
  :root[data-theme="light"] .hlxh{--paper:#FFFFFF;--ink:#23262C;--ink-2:#5C626B;--ink-3:#8A9099;--accent:#2B3648}
  :root[data-theme="white"] .hlxh{--paper:#FFFFFF;--ink:#1A1C22;--ink-2:#565B63;--ink-3:#8A9099;--accent:#2B3648}
  :root[data-theme="dark"] .hc{--paper:#14130F;--paper2:#1c1b16;--card:#1b1a15;--ink:#EDEAE1;--ink-64:rgba(237,234,225,.66);--ink-44:rgba(237,234,225,.46);--line:rgba(237,234,225,.14);--line-soft:rgba(237,234,225,.08);--accent:#9CB0D0;--accent-2:#7688a6;--good:#7cc79a}
  :root[data-theme="light"] .hc{--paper:#F2F0EB;--paper2:#E9E6DF;--card:#FBFAF7;--ink:#191712;--ink-64:rgba(25,23,18,.64);--ink-44:rgba(25,23,18,.44);--line:rgba(25,23,18,.13);--line-soft:rgba(25,23,18,.08);--accent:#2B3648;--accent-2:#55647E;--good:#2f6b46}
  :root[data-theme="white"] .hc{--paper:#FFFFFF;--paper2:#F4F4F5;--card:#FFFFFF;--ink:#191712;--ink-64:rgba(25,23,18,.64);--ink-44:rgba(25,23,18,.44);--line:rgba(25,23,18,.12);--line-soft:rgba(25,23,18,.06);--accent:#2B3648;--accent-2:#55647E;--good:#2f6b46}
  :root[data-theme="dark"] .hsg{--paper:#14130F;--paper2:#1c1b16;--card:#1b1a15;--ink:#EDEBE4;--ink-2:#A7A399;--ink-3:#7C786E;--line:rgba(237,235,228,.15);--line-soft:rgba(237,235,228,.08);--accent:#9CB0D0;--good:#7cc79a}
  :root[data-theme="light"] .hsg{--paper:#F2F0EB;--paper2:#EAE7E0;--card:#FBFAF7;--ink:#191712;--ink-2:#5C626B;--ink-3:#8A9099;--line:rgba(25,23,18,.13);--line-soft:rgba(25,23,18,.07);--accent:#2B3648;--good:#2f6b46}
  :root[data-theme="white"] .hsg{--paper:#FFFFFF;--paper2:#F4F4F5;--card:#FFFFFF;--ink:#191712;--ink-2:#565B63;--ink-3:#8A9099;--line:rgba(20,22,28,.12);--line-soft:rgba(20,22,28,.06);--accent:#2B3648;--good:#2f6b46}
  :root[data-theme="dark"] .hlxc{--paper:#14130F;--ink:#EDEAE1;--ink-64:rgba(237,234,225,.66);--ink-40:rgba(237,234,225,.42);--line:rgba(237,234,225,.14);--accent:#9CB0D0}
  :root[data-theme="light"] .hlxc{--paper:#F2F0EB;--ink:#191712;--ink-64:rgba(25,23,18,.60);--ink-40:rgba(25,23,18,.40);--line:rgba(25,23,18,.12);--accent:#2B3648}
  :root[data-theme="white"] .hlxc{--paper:#FFFFFF;--ink:#191712;--ink-64:rgba(25,23,18,.60);--ink-40:rgba(25,23,18,.40);--line:rgba(25,23,18,.10);--accent:#2B3648}
  /* === тонкий разделитель между блоками: ОДНА линия во всю ширину на границе === */
  .hlxh,.hlxc,.hc,.hpr,.hf{border-top:1px solid color-mix(in srgb,currentColor 8%,transparent)}
  /* гасим внутренние/контентные дубли-линии, чтобы не было двойных и разной ширины */
  .ha .about-social,.hf .foot{border-top:none!important}
</style>
<div class="hh">
  <div class="top"><div class="wrap">
    <span>Размеры 42–74</span><span class="sep"></span><span>Доставка по РФ от 2 дней</span><span class="sep"></span><span>Возврат 14 дней</span><span class="sep"></span><span>Оплата картой или СБП</span>
  </div></div>
  <div class="bar"><div class="wrap hd">
    <button class="burg" id="hhBurg" aria-label="Меню" aria-expanded="false"><span></span></button>
    <a class="logo" href="/"><i>Hello</i>HAILEY</a>
    <nav>
      <a href="/catalog?set=%D0%A5%D0%98%D0%A2">Хиты</a><a href="/catalog">Каталог</a><a href="/catalog?set=%D0%9D%D0%9E%D0%92%D0%98%D0%9D%D0%9A%D0%90">Новинки</a><a href="/catalog?set=%D0%A0%D0%90%D0%A1%D0%9F%D0%A0%D0%9E%D0%94%D0%90%D0%96%D0%90">Акции</a><a href="/kak-vybrat-razmer">Подобрать размер</a><a href="/page156060236.html#delivery">Доставка</a><a class="nx" href="#contacts">Контакты</a><a class="nx" href="#brand">О бренде</a>
    </nav>
    <div class="htools">
      <button class="theme" id="hhTheme" aria-label="Тема">◐</button>
      <form class="s" style="display:flex;align-items:center;margin:0" role="search" autocomplete="off" onsubmit="return hhGo(event)"><input name="q" id="hhQ" type="search" placeholder="Поиск" aria-label="Поиск по каталогу" autocomplete="off" style="width:110px;max-width:34vw;padding:7px 13px;border:1px solid var(--line,rgba(25,23,18,.18));border-radius:20px;background:transparent;color:inherit;font-family:inherit;font-size:13px;letter-spacing:.02em;outline:none;transition:width .2s,border-color .2s" onfocus="this.style.width='150px';this.style.borderColor='var(--accent,#2B3648)'" onblur="this.style.width='110px';this.style.borderColor='var(--line,rgba(25,23,18,.18))'"><div class="hh-sug" id="hhSug" role="listbox"></div></form>
      <a class="favlink" id="hhFav" href="/izbrannoe" aria-label="Избранное"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 20.7l-1.4-1.3C5.4 14.6 2 11.5 2 7.8 2 5 4.2 3 6.9 3c1.6 0 3.1.7 4.1 1.9C12 3.7 13.5 3 15.1 3 17.8 3 20 5 20 7.8c0 3.7-3.4 6.8-8.6 11.6L12 20.7z"/></svg><span class="fc" id="hhfc">0</span></a>
      <button class="cartlink" id="hhCart"><svg class="cart-ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 8h12l-1 12H7L6 8z"/><path d="M9 8V6a3 3 0 0 1 6 0v2"/></svg><span class="ct">Корзина</span> <span class="cc" id="hhcc">0</span></button>
    </div>
  </div></div>
  <div class="ovl" id="hhOvl"></div>
  <nav class="mnu" id="hhMnu" aria-label="Меню сайта">
    <div class="mh"><a class="mlogo" href="/"><i>Hello</i>HAILEY</a><button class="cl" id="hhMnuCl" aria-label="Закрыть">✕</button></div>
    <a class="mfav" href="/izbrannoe"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 20.7l-1.4-1.3C5.4 14.6 2 11.5 2 7.8 2 5 4.2 3 6.9 3c1.6 0 3.1.7 4.1 1.9C12 3.7 13.5 3 15.1 3 17.8 3 20 5 20 7.8c0 3.7-3.4 6.8-8.6 11.6L12 20.7z"/></svg>Избранное<span class="fc" id="hhmfc">0</span></a>
    <a href="/catalog">Каталог</a>
    <a href="/catalog?set=%D0%A5%D0%98%D0%A2">Хиты</a>
    <a href="/catalog?set=%D0%9D%D0%9E%D0%92%D0%98%D0%9D%D0%9A%D0%90">Новинки</a>
    <a href="/catalog?set=%D0%A0%D0%90%D0%A1%D0%9F%D0%A0%D0%9E%D0%94%D0%90%D0%96%D0%90">Акции</a>
    <a href="/kak-vybrat-razmer">Подобрать размер</a>
    <a href="/dostavka-i-oplata">Доставка и оплата</a>
    <a href="/page156060236.html#faq">Частые вопросы</a>
    <div class="grp">Мы на связи</div>
    <div class="cts">
      <a href="tel:+79935910371">+7 993 591-03-71</a>
      <a href="https://t.me/hailey_mgr" target="_blank" rel="noopener">Telegram @hailey_mgr</a>
      <a href="https://www.instagram.com/hailey.looks/" target="_blank" rel="noopener">Instagram @hailey.looks</a>
    </div>
    <div class="grp">Тема оформления</div>
    <div class="thm" id="hhThm">
      <button type="button" data-t="light">Бежевая</button>
      <button type="button" data-t="white">Светлая</button>
      <button type="button" data-t="dark">Тёмная</button>
    </div>
  </nav>
</div>
<div class="hh-sp"></div>
<script>
(function(){
  var order=['light','white','dark'];
  try{var s=localStorage.getItem('hailey-theme');if(s)document.documentElement.setAttribute('data-theme',s);}catch(e){}
  // Тема ставится из двух мест: иконка-цикл в шапке (десктоп) и явный выбор в меню (телефон) —
  // поэтому одна функция, чтобы подсветка активной кнопки не разъезжалась с реальной темой.
  var thm=document.getElementById('hhThm');
  function markTheme(n){
    if(!thm)return;
    var bs=thm.querySelectorAll('button');
    for(var i=0;i<bs.length;i++)bs[i].classList.toggle('on',bs[i].getAttribute('data-t')===n);
  }
  function setTheme(n){
    document.documentElement.setAttribute('data-theme',n);
    try{localStorage.setItem('hailey-theme',n);}catch(e){}
    markTheme(n);
  }
  // без явного выбора сайт идёт за системной темой (@media prefers-color-scheme),
  // поэтому подсвечиваем её же — иначе у гостя с тёмным телефоном горит «Бежевая» на тёмном сайте
  function curTheme(){
    var a=document.documentElement.getAttribute('data-theme');
    if(a)return a;
    return (window.matchMedia&&window.matchMedia('(prefers-color-scheme:dark)').matches)?'dark':'light';
  }
  markTheme(curTheme());
  if(thm)thm.addEventListener('click',function(e){
    var b=e.target.closest('button[data-t]');
    if(b)setTheme(b.getAttribute('data-t'));   // меню не закрываем: тему видно сразу, можно перещёлкнуть
  });
  var t=document.getElementById('hhTheme');
  if(t)t.addEventListener('click',function(){var i=order.indexOf(curTheme());if(i<0)i=0;setTheme(order[(i+1)%order.length]);});
  // Бургер-меню (телефон): на ≤820px обычная навигация скрыта, без него разделов не найти.
  var bg=document.getElementById('hhBurg'),mn=document.getElementById('hhMnu'),ov=document.getElementById('hhOvl');
  function menu(open){
    if(!mn)return;
    mn.classList.toggle('on',open);ov.classList.toggle('on',open);
    if(bg)bg.setAttribute('aria-expanded',open?'true':'false');
    document.body.style.overflow=open?'hidden':'';   // фон не должен скроллиться под панелью
  }
  if(bg)bg.addEventListener('click',function(){menu(!mn.classList.contains('on'));});
  if(ov)ov.addEventListener('click',function(){menu(false);});
  var cl=document.getElementById('hhMnuCl');
  if(cl)cl.addEventListener('click',function(){menu(false);});
  if(mn)mn.addEventListener('click',function(e){if(e.target.closest('a'))menu(false);});
  document.addEventListener('keydown',function(e){if(e.key==='Escape')menu(false);});

  var c=document.getElementById('hhCart');
  if(c)c.addEventListener('click',function(e){e.preventDefault();var o=document.getElementById('openCart'),b=document.getElementById('cartBtn');if(o){o.click();}else if(b){b.click();}else{location.href='/catalog?cart=1';}});
  // «Оформить заказ» в любой корзине (каталог/PDP) -> отдельная страница /checkout.
  // Перехват в ФАЗЕ ЗАХВАТА: срабатывает раньше родного обработчика блока, поэтому модалка не открывается.
  document.addEventListener('click',function(e){
    var t=e.target&&e.target.closest?e.target.closest('#toCheckout'):null;
    if(!t)return;
    var n=0;try{n=(JSON.parse(localStorage.getItem('hailey_cart_v1'))||[]).length;}catch(err){}
    if(!n)return;
    e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();
    location.href='/checkout';
  },true);
  // страница без drawer (гид, доставка, контакты) -> уводим в каталог и там сразу открываем корзину
  try{if(/[?&]cart=1/.test(location.search)){var _t=0,_iv=setInterval(function(){var x=document.getElementById('openCart')||document.getElementById('cartBtn');if(x){x.click();clearInterval(_iv);}else if(++_t>40){clearInterval(_iv);}},250);}}catch(e){}
  var cc=document.getElementById('hhcc');
  // счётчики корзины и избранного: локального события об изменении localStorage в СВОЁЙ вкладке нет,
  // поэтому (как и корзина с самого начала) просто переопрашиваем — источник правды один на весь сайт
  var fcs=[document.getElementById('hhfc'),document.getElementById('hhmfc')],favLink=document.getElementById('hhFav');
  function updFav(){try{
    var f=JSON.parse(localStorage.getItem('hailey_fav_v1'))||[];
    fcs.forEach(function(el){if(el){el.textContent=f.length;el.classList.toggle('on',f.length>0);}});
    if(favLink)favLink.classList.toggle('has',f.length>0);
  }catch(e){}}
  function upd(){try{var a=JSON.parse(localStorage.getItem('hailey_cart_v1'))||[];var q=a.reduce(function(s,i){return s+(i.qty||0);},0);if(cc){cc.textContent=q;cc.classList.toggle('on',q>0);}}catch(e){}updFav();}
  upd();
  window.addEventListener('storage',function(e){if(e.key==='hailey_cart_v1'||e.key==='hailey_fav_v1')upd();});
  setInterval(upd,1200);
  // Липкая шапка: тонкая плашка .top прокручивается и уходит; фиксируется только .bar (лого/меню/корзина).
  // Распорка .hh-sp = высоте .bar только когда .bar зафиксирован → нет прыжка контента.
  var _bar=document.querySelector('.hh .bar'),_top=document.querySelector('.hh .top'),_sp=document.querySelector('.hh-sp');
  function _stick(){var th=_top?_top.offsetHeight:0;var st=(window.pageYOffset||document.documentElement.scrollTop)>=th;if(_bar)_bar.classList.toggle('stuck',st);if(_sp)_sp.style.height=st?(_bar.offsetHeight+'px'):'0px';}
  window.addEventListener('scroll',_stick,{passive:true});window.addEventListener('resize',_stick);_stick();setTimeout(_stick,400);setTimeout(_stick,1200);

  // === Живой поиск с подсказками (мини-фото + название + цена) ===
  // Индекс товаров тянем из raw GitHub (тот же репо, что каталог) — шапка остаётся без вшитых данных, авто-синхрон.
  var RAW='https://raw.githubusercontent.com/Hellohailey25/hailey-assets/main/search.json';
  var qI=document.getElementById('hhQ'),sug=document.getElementById('hhSug');
  if(qI&&sug){
    var IDX=null,idxLoad=null,items=[],act=-1;
    function _n(s){return (s||'').toLowerCase().replace(/ё/g,'е');}
    var _ENDS=['ыми','ими','ого','его','ому','ему','ами','ями','ах','ях','ый','ий','ой','ая','яя','ое','ее','ые','ие','ых','их','ом','ем','ов','ев','ам','ям','ей','ю','я','ы','и','е','о','а','й','ь','у'];
    function _stem(w){w=_n(w);for(var i=0;i<_ENDS.length;i++){var s=_ENDS[i];if(w.length-s.length>=3&&w.slice(-s.length)===s)return w.slice(0,-s.length);}return w;}
    var _SYN={'деним':['джинс'],'джинс':['деним']};
    function _toks(s){return _n(s).split(/[^a-zа-я0-9]+/).filter(Boolean);}
    function matchQ(hay,q){var H=_toks(hay).map(_stem);
      return _toks(q).map(_stem).every(function(qs){if(!qs)return true;var cand=[qs].concat(_SYN[qs]||[]);
        return cand.some(function(cs){var num=/^\d/.test(cs);return H.some(function(hs){return num?hs.indexOf(cs)===0:(hs.indexOf(cs)===0||(hs.length>=3&&cs.indexOf(hs)===0));});});});}
    function rub(n){return (n||0).toLocaleString('ru-RU')+' руб';}
    function loadIdx(){if(idxLoad)return idxLoad;idxLoad=fetch(RAW,{cache:'no-store'}).then(function(r){return r.json();})
      .then(function(a){IDX=Array.isArray(a)?a:[];return IDX;}).catch(function(){IDX=[];return IDX;});return idxLoad;}
    function render(){
      var q=(qI.value||'').trim();act=-1;
      if(!IDX||!q){sug.classList.remove('on');return;}
      items=IDX.filter(function(p){return matchQ(p.s||p.n,q);}).slice(0,6);
      if(!items.length){sug.innerHTML='<div class="snone">Ничего не найдено</div><div class="sall" data-all>Смотреть весь каталог →</div>';sug.classList.add('on');return;}
      sug.innerHTML=items.map(function(p,i){return '<a class="si" data-i="'+i+'" href="'+p.u+'"><img src="'+p.i+'" alt="" loading="lazy"><div><div class="sn">'+p.n+'</div><div class="sp">'+rub(p.p)+'</div></div></a>';}).join('')
        +'<div class="sall" data-all>Все результаты по «'+q+'» →</div>';
      sug.classList.add('on');
    }
    var dt;
    qI.addEventListener('focus',function(){loadIdx().then(render);});
    qI.addEventListener('input',function(){clearTimeout(dt);dt=setTimeout(render,110);});
    qI.addEventListener('keydown',function(e){
      if(e.key==='ArrowDown'||e.key==='ArrowUp'){if(!items.length)return;e.preventDefault();
        act=(act+(e.key==='ArrowDown'?1:-1)+items.length)%items.length;
        [].forEach.call(sug.querySelectorAll('.si'),function(el,i){el.classList.toggle('act',i===act);});}
      else if(e.key==='Enter'){if(act>=0&&items[act]){e.preventDefault();location.href=items[act].u;}}
      else if(e.key==='Escape'){sug.classList.remove('on');}
    });
    sug.addEventListener('mousedown',function(e){var all=e.target.closest('[data-all]');if(all){e.preventDefault();
      var v=(qI.value||'').trim();location.href='/catalog'+(v?('?q='+encodeURIComponent(v)):'');}});
    document.addEventListener('click',function(e){if(!e.target.closest('.hh .s'))sug.classList.remove('on');});
  }
  window.hhGo=function(ev){ev.preventDefault();var v=(qI&&qI.value||'').trim();if(v)location.href='/catalog?q='+encodeURIComponent(v);return false;};
})();

/* =========================================================================
   Настоящие отзывы в карточке товара.
   В шаблоне PDP были ЗАГЛУШКИ — выдуманные «Марина/Ольга/Ирина» и «946 оценок».
   Тянем реальные из reviews.json (собирает wb_reviews.py по Feedbacks API WB).
   Живёт в сквозной шапке намеренно: иначе правку пришлось бы раскатывать
   на 17 карточек по отдельности при каждом изменении.
   ========================================================================= */
function hpReviewsInit(){
  var box=document.querySelector('.hp .revs'); if(!box) return;      // не карточка товара
  var SRC='https://raw.githubusercontent.com/Hellohailey25/hailey-assets/main/reviews.json';
  var MAX=20;

  function sku(){
    try{
      var s=document.querySelector('.hp ~ script[type="application/ld+json"], script[type="application/ld+json"]');
      var all=document.querySelectorAll('script[type="application/ld+json"]');
      for(var i=0;i<all.length;i++){
        var j=JSON.parse(all[i].textContent||'{}');
        if(j.sku) return String(j.sku);
      }
    }catch(e){}
    var m=location.pathname.match(/(\d{6,})\/?$/);                    // запасной путь: артикул в конце адреса
    return m?m[1]:'';
  }
  function esc(s){return (s||'').replace(/[<>&]/g,function(c){return {'<':'&lt;','>':'&gt;','&':'&amp;'}[c];});}
  function ru(d){var m=['янв','фев','мар','апр','мая','июн','июл','авг','сен','окт','ноя','дек'],p=(d||'').split('-');
    return p.length<3?'':parseInt(p[2],10)+' '+m[parseInt(p[1],10)-1]+' '+p[0];}

  var css=document.createElement('style');
  css.textContent=
   '.hp .revs{display:flex!important;gap:14px;overflow-x:auto;scroll-snap-type:x mandatory;'+
   'padding-bottom:10px;-webkit-overflow-scrolling:touch;scrollbar-width:thin}'+
   '.hp .revs .rev{flex:0 0 300px;scroll-snap-align:start;background:var(--card);border:1px solid var(--line);'+
   'border-radius:12px;padding:16px;display:flex;flex-direction:column}'+
   '@media(max-width:600px){.hp .revs .rev{flex-basis:78vw}}'+
   '.hp .revs .rev .rhead{display:flex;justify-content:space-between;align-items:baseline;gap:10px}'+
   '.hp .revs .rev .rd{font-size:11.5px;color:var(--ink-3);white-space:nowrap}'+
   '.hp .revs .rev p{font-size:13.5px;line-height:1.5;color:var(--ink-2);margin-top:8px}'+
   '.hp .revs .rev .rph{display:flex;gap:6px;margin-top:11px;flex-wrap:wrap}'+
   '.hp .revs .rev .rph img{width:58px;height:74px;object-fit:cover;border-radius:6px;cursor:zoom-in;background:var(--paper2)}'+
   '.hp .revs .rev .rz{font-size:11.5px;color:var(--ink-3);margin-top:4px}'+
   '.hp .revlb{position:fixed;inset:0;background:rgba(0,0,0,.9);z-index:99999;display:none;'+
   'align-items:center;justify-content:center;padding:20px}'+
   '.hp .revlb.on{display:flex}.hp .revlb img{max-width:100%;max-height:92vh;border-radius:8px}';
  document.head.appendChild(css);

  box.innerHTML='<div style="color:var(--ink-3);font-size:13.5px;padding:6px 0">Загружаем отзывы…</div>';

  fetch(SRC+'?t='+Date.now()).then(function(r){return r.json();}).then(function(d){
    var nm=sku(), list=(d.reviews||{})[nm]||[], agg=(d.products||{})[nm]||null;

    /* Разметка для поисковиков: в шаблоне у ВСЕХ карточек стоял один выдуманный
       рейтинг 4.9/946. Фиктивный aggregateRating — прямой повод понизить сайт в выдаче. */
    if(agg){
      try{
        var lds=document.querySelectorAll('script[type="application/ld+json"]');
        for(var k=0;k<lds.length;k++){
          var obj=JSON.parse(lds[k].textContent||'{}');
          if(obj['@type']==='Product'&&obj.aggregateRating){
            obj.aggregateRating.ratingValue=String(agg.avg);
            obj.aggregateRating.reviewCount=String(agg.count);
            lds[k].textContent=JSON.stringify(obj);
          }
        }
      }catch(e){}
    }

    var top=document.querySelector('.hp .rev-top');
    if(top&&agg){
      top.innerHTML='<span class="num">'+String(agg.avg).replace('.',',')+'</span>'+
        '<div><div class="stars">'+'★'.repeat(Math.round(agg.avg))+'</div>'+
        '<div class="sub" style="margin:0">'+agg.count.toLocaleString('ru-RU')+' оценок покупательниц</div></div>';
    }else if(top){ top.style.display='none'; }

    if(!list.length){
      box.innerHTML='<div style="color:var(--ink-3);font-size:13.5px;padding:6px 0">Отзывы на этот товар скоро появятся.</div>';
    }else{
      var h='';
      list.slice(0,MAX).forEach(function(r){
        h+='<div class="rev"><div class="rhead"><div><div class="rs">'+'★'.repeat(r.val)+'</div>'+
           '<div class="rn">'+esc(r.user)+'</div>'+
           (r.size?'<div class="rz">размер '+esc(r.size)+'</div>':'')+'</div>'+
           '<div class="rd">'+ru(r.date)+'</div></div>'+
           (r.text?'<p>'+esc(r.text)+'</p>':'');
        if((r.photos||[]).length){
          h+='<div class="rph">';
          r.photos.forEach(function(p){h+='<img loading="lazy" src="'+p+'" alt="Фото из отзыва">';});
          h+='</div>';
        }
        h+='</div>';
      });
      box.innerHTML=h;
    }

    var link=document.querySelector('.hp .revlink');
    if(link){
      link.setAttribute('href','/otzyvy');                 // раньше вело на Wildberries — уводили покупателя с сайта
      link.removeAttribute('target'); link.removeAttribute('rel');
      link.textContent='Все отзывы покупательниц →';
    }

    var lb=document.createElement('div'); lb.className='revlb';
    lb.innerHTML='<img alt="Фото из отзыва">';
    document.querySelector('.hp').appendChild(lb);
    box.addEventListener('click',function(e){
      var i=e.target.closest('.rph img'); if(!i)return;
      lb.querySelector('img').src=i.src; lb.classList.add('on');
    });
    lb.addEventListener('click',function(){lb.classList.remove('on');});
    document.addEventListener('keydown',function(e){if(e.key==='Escape')lb.classList.remove('on');});
  }).catch(function(){
    box.innerHTML='<div style="color:var(--ink-3);font-size:13.5px;padding:6px 0">Отзывы временно недоступны.</div>';
  });
}
/* ГРАБЛИ: шапка идёт на странице ПЕРВОЙ и её скрипт выполняется до того, как
   отрисован блок карточки товара — querySelector('.hp .revs') возвращал null
   и функция молча выходила, оставляя на сайте выдуманные отзывы. */
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hpReviewsInit);
else hpReviewsInit();
</script>
